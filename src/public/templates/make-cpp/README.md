# Cpp + Makefile template

## Requirements

- GCC
- Make
- Vscode C++ Extension

## Usage

- Build: `make`
- Build and Run: `make dev`
- Create hpp and cpp files: `make new <name>`

Note : Change compiler path in `C_cpp_properties.json` if needed.
